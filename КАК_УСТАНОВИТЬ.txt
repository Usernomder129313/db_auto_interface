═══════════════════════════════════════════════════════════════════════════════
           КАК УСТАНОВИТЬ БИБЛИОТЕКУ НА ДРУГОМ УСТРОЙСТВЕ
═══════════════════════════════════════════════════════════════════════════════

📦 ВАРИАНТ 1: УСТАНОВКА ИЗ АРХИВА (РЕКОМЕНДУЕТСЯ)
═══════════════════════════════════════════════════════════════════════════════

ШАГ 1: Соберите пакет на исходном устройстве
───────────────────────────────────────────────────────────────────────────────
   cd db_auto_interface_package
   python -m build
   
   Или:
   python setup.py sdist bdist_wheel
   
   Это создаст папку dist/ с файлами:
   - db-auto-interface-1.0.0.tar.gz
   - db_auto_interface-1.0.0-py3-none-any.whl

ШАГ 2: Скопируйте архив на другое устройство
───────────────────────────────────────────────────────────────────────────────
   Скопируйте файл dist/db-auto-interface-1.0.0.tar.gz

ШАГ 3: Установите на новом устройстве
───────────────────────────────────────────────────────────────────────────────
   pip install db-auto-interface-1.0.0.tar.gz
   
   Или из wheel:
   pip install db_auto_interface-1.0.0-py3-none-any.whl

ШАГ 4: Проверьте установку
───────────────────────────────────────────────────────────────────────────────
   python -c "from db_auto_interface import DBAutoInterface; print('OK')"

═══════════════════════════════════════════════════════════════════════════════
📦 ВАРИАНТ 2: УСТАНОВКА ИЗ ИСХОДНИКОВ
═══════════════════════════════════════════════════════════════════════════════

ШАГ 1: Скопируйте всю папку db_auto_interface_package
───────────────────────────────────────────────────────────────────────────────
   Скопируйте папку целиком на другое устройство

ШАГ 2: Установите
───────────────────────────────────────────────────────────────────────────────
   cd db_auto_interface_package
   pip install .
   
   Или в режиме разработки:
   pip install -e .

ШАГ 3: Проверьте
───────────────────────────────────────────────────────────────────────────────
   python -c "from db_auto_interface import DBAutoInterface; print('OK')"

═══════════════════════════════════════════════════════════════════════════════
📦 ВАРИАНТ 3: ЧЕРЕЗ GIT (ЕСЛИ РЕПОЗИТОРИЙ)
═══════════════════════════════════════════════════════════════════════════════

   pip install git+https://github.com/yourusername/db_auto_interface.git
   
   Или конкретная ветка:
   pip install git+https://github.com/yourusername/db_auto_interface.git@main

═══════════════════════════════════════════════════════════════════════════════
📦 ВАРИАНТ 4: ЧЕРЕЗ PyPI (ЕСЛИ ОПУБЛИКОВАНА)
═══════════════════════════════════════════════════════════════════════════════

   pip install db-auto-interface
   
   Или конкретная версия:
   pip install db-auto-interface==1.0.0

═══════════════════════════════════════════════════════════════════════════════
✅ ПРОВЕРКА ПОСЛЕ УСТАНОВКИ
═══════════════════════════════════════════════════════════════════════════════

1. Проверьте импорт:
   python -c "from db_auto_interface import DBAutoInterface, UniversalDB; print('Импорты OK')"

2. Проверьте версию:
   python -c "from db_auto_interface import __version__; print(f'Версия: {__version__}')"

3. Проверьте установку через pip:
   pip list | grep db-auto-interface

4. Проверьте консольную команду (если установлена):
   db-auto-interface --help

5. Запустите пример:
   python -c "from db_auto_interface import DBAutoInterface; print('Библиотека работает!')"

═══════════════════════════════════════════════════════════════════════════════
🔧 ИСПОЛЬЗОВАНИЕ ПОСЛЕ УСТАНОВКИ
═══════════════════════════════════════════════════════════════════════════════

После установки библиотека доступна из любого места:

   from db_auto_interface import DBAutoInterface
   
   app = DBAutoInterface(connection_params={
       'host': 'localhost',
       'database': 'your_db',
       'user': 'postgres',
       'password': 'password',
       'port': 5432
   })
   app.run()

═══════════════════════════════════════════════════════════════════════════════
📝 ПРИМЕРЫ КОМАНД
═══════════════════════════════════════════════════════════════════════════════

# Сборка пакета
cd db_auto_interface_package
python -m build

# Установка из архива
pip install dist/db-auto-interface-1.0.0.tar.gz

# Установка из исходников
pip install .

# Установка в режиме разработки
pip install -e .

# Удаление библиотеки
pip uninstall db-auto-interface

# Обновление библиотеки
pip install --upgrade db-auto-interface

═══════════════════════════════════════════════════════════════════════════════

