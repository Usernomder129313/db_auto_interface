═══════════════════════════════════════════════════════════════════════════════
                    БЫСТРЫЙ СТАРТ: СОЗДАНИЕ И УСТАНОВКА ПАКЕТА
═══════════════════════════════════════════════════════════════════════════════

🚀 ШАГ 1: СОЗДАНИЕ ПАКЕТА (НА ИСХОДНОМ УСТРОЙСТВЕ)
═══════════════════════════════════════════════════════════════════════════════

1. Перейдите в папку пакета:
   cd db_auto_interface_package

2. Установите инструменты сборки:
   pip install build wheel

3. Соберите пакет:
   python -m build

4. Проверьте результат:
   dir dist
   
   Должны быть файлы:
   - db-auto-interface-1.0.0.tar.gz
   - db_auto_interface-1.0.0-py3-none-any.whl

═══════════════════════════════════════════════════════════════════════════════
📦 ШАГ 2: ПЕРЕНОС НА ДРУГОЕ УСТРОЙСТВЕ
═══════════════════════════════════════════════════════════════════════════════

ВАРИАНТ A: Только архив (минимальный размер)
───────────────────────────────────────────────────────────────────────────────
   Скопируйте файл: dist/db-auto-interface-1.0.0.tar.gz

ВАРИАНТ B: Вся папка (для разработки)
───────────────────────────────────────────────────────────────────────────────
   Скопируйте всю папку: db_auto_interface_package

═══════════════════════════════════════════════════════════════════════════════
💾 ШАГ 3: УСТАНОВКА НА НОВОМ УСТРОЙСТВЕ
═══════════════════════════════════════════════════════════════════════════════

ВАРИАНТ A: Из архива
───────────────────────────────────────────────────────────────────────────────
   pip install db-auto-interface-1.0.0.tar.gz

ВАРИАНТ B: Из исходников
───────────────────────────────────────────────────────────────────────────────
   cd db_auto_interface_package
   pip install .

═══════════════════════════════════════════════════════════════════════════════
✅ ШАГ 4: ПРОВЕРКА УСТАНОВКИ
═══════════════════════════════════════════════════════════════════════════════

1. Проверьте импорт:
   python -c "from db_auto_interface import DBAutoInterface; print('OK')"

2. Проверьте версию:
   python -c "from db_auto_interface import __version__; print(__version__)"

3. Проверьте установку:
   pip list | grep db-auto-interface

4. Используйте в коде:
   from db_auto_interface import DBAutoInterface
   
   app = DBAutoInterface(connection_params={
       'host': 'localhost',
       'database': 'test_db',
       'user': 'postgres',
       'password': 'password'
   })
   app.run()

═══════════════════════════════════════════════════════════════════════════════
📋 КОМАНДЫ В ОДНОЙ СТРОКЕ
═══════════════════════════════════════════════════════════════════════════════

СОЗДАНИЕ:
   cd db_auto_interface_package && pip install build wheel && python -m build

УСТАНОВКА ИЗ АРХИВА:
   pip install dist/db-auto-interface-1.0.0.tar.gz

УСТАНОВКА ИЗ ИСХОДНИКОВ:
   cd db_auto_interface_package && pip install .

ПРОВЕРКА:
   python -c "from db_auto_interface import DBAutoInterface; print('OK')"

═══════════════════════════════════════════════════════════════════════════════

